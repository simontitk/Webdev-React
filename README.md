# Webdev-React

### Setup
`git submodule init`
`git submodule update --remote`
`npm install`
`cd API`
`npm install`

### To start the app

Fullstack:
`npm run fullstart`

Frontend:
`npm run dev`

Backend:
`npm run backend`
